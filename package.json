{
  "name": "wke-platform",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "build": "cross-env WKE_MANAGED_HOSTING=true npm run build --workspace=web",
    "start": "cross-env WKE_MANAGED_HOSTING=true npm run start --workspace=web"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
