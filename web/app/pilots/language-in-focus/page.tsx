import { LanguageInFocusPilot } from "@/components/pilots/LanguageInFocusPilot";

export const metadata = {
  title: "Language in focus — Pilot",
  description: "Interactive language-in-focus activity for like + -ing",
};

export default function LanguageInFocusPilotPage() {
  return <LanguageInFocusPilot />;
}
