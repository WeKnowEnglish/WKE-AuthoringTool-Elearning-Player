import { MediaPickerPilot } from "@/components/pilots/MediaPickerPilot";

export const metadata = {
  title: "Media picker — Pilot",
  description: "Preview School / Mine / Linked shelves for the teacher media library picker",
};

export default function MediaPickerPilotPage() {
  return <MediaPickerPilot />;
}
