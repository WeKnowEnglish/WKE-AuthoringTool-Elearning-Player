import { SelfStudyPacksPilot } from "@/components/pilots/SelfStudyPacksPilot";

export const metadata = {
  title: "Self Study packs — Pilot",
  description: "Catalog shell for Primary Self Study lesson packs",
};

export default function SelfStudyPacksPilotPage() {
  return <SelfStudyPacksPilot />;
}
