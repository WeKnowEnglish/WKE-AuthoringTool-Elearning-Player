import { notFound } from "next/navigation";

/** Activity library archived — teacher quiz generator and table import removed from nav. */
export default function TeacherActivitiesPage() {
  notFound();
}
