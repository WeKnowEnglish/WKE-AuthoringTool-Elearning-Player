/**
 * @deprecated Prefer composing landing sections from the homepage page.
 * Kept so older imports remain discoverable.
 */
export { LandingPathPicker as LevelLandingClient } from "@/components/landing/LandingPathPicker";
