/**
 * @deprecated Secondary portal chrome lives in `SecondaryPortalShell`
 * (top bar + side nav + sound/logout at the side panel bottom).
 * Header/mobile nav helpers were removed when Secondary matched Primary layout.
 */

export {};
