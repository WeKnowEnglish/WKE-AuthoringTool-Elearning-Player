/**
 * @deprecated Import from `@/lib/teacher-theme` — kept as LTC aliases.
 */
export {
  TEACHER_THEME_MODES as LTC_THEME_MODES,
  TEACHER_THEME_TINTS as LTC_THEME_TINTS,
  TEACHER_THEME_DEFAULT as LTC_THEME_DEFAULT,
  TEACHER_MODE_LABELS as LTC_MODE_LABELS,
  TEACHER_TINT_LABELS as LTC_TINT_LABELS,
  resolveTeacherThemeCssVars as resolveLtcThemeCssVars,
  readTeacherThemeSelection as readLtcThemeSelection,
  persistTeacherThemeSelection as persistLtcThemeSelection,
  type TeacherThemeMode as LtcThemeMode,
  type TeacherThemeTint as LtcThemeTint,
  type TeacherThemeSelection as LtcThemeSelection,
} from "@/lib/teacher-theme";
