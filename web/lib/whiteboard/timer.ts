/** Whiteboard timer — shared collaborative-activity contract. */
export {
  addTime,
  expireTimer,
  formatRemaining,
  pauseTimer,
  remainingMs,
  resetTimer,
  resumeTimer,
  startTimer,
} from "@/lib/collaborative-activity/timer";
